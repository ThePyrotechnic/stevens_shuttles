import pytest

pytest.skip('Currently no way to test gen_schedules.py', allow_module_level=True)


# TODO Clean up gen_schedules.py so that it can be moved into the lib
class TestGenSchedules:
    pass
